API base URL is handled by proxy — no env var needed.
Local dev: vite.config.ts proxies /api/* → https://api.samadhantra.com/api/*
Production: vercel.json rewrites /api/* → https://api.samadhantra.com/api/*
VITE_API_BASE_URL is intentionally removed to prevent bypassing the proxy.